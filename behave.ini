# Behave Configuration
[behave]
paths = features
format = pretty
show_snippets = true
wip = false

# Tags
[behave:tags]
@data = Data loading and export tests
@backup = Backup management tests
@players = Player management tests
@tournaments = Tournament tests
@validation = Data validation tests
